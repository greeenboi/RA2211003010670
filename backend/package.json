{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "format": "biome format --write",
		"lint": "biome lint --write"
  },
  "dependencies": {
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/bun": "latest"
  }
}